import 'package:flutter/material.dart';

class DeleteBanner extends StatelessWidget {
  const DeleteBanner({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}